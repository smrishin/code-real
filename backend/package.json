{
  "name": "backend",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "dev": "node app.js",
    "start": "node app.js",
    "build": "echo 'No build step required'",
    "setup-vercel-env": "node scripts/setup-vercel-env.js",
    "vercel-deploy-dev": "npm run setup-vercel-env && vercel",
    "vercel-deploy-prod": "npm run setup-vercel-env && vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@google/genai": "^1.5.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  }
}
